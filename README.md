This is my app. Uh, it kinda sucks, lowkey dont know how to deploy it so I guess it will be sitting here for a bit

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
